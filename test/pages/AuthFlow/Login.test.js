import { describe, it, expect } from "vitest";
import Login from "../../../src/pages/AuthFlow/Login";

describe("Login Form", () => {
	it("should show the login page", () => {
		expect(Login).toBe();
	});
});
